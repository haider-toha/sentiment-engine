# =============================================================================
# IMPORTANT: Install PyTorch FIRST before installing these requirements!
# =============================================================================
# For RTX 50 series (Blackwell/sm_120), you need PyTorch nightly with CUDA 13.0:
#
#   pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu130
#
# Then install the rest:
#   pip install -r requirements.txt
#
# (torch is excluded below so it won't overwrite your nightly build)
# =============================================================================

# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
psycopg2-binary>=2.9.9

# Data collection
feedparser>=6.0.10
praw>=7.7.1
httpx>=0.26.0
beautifulsoup4>=4.12.3
newspaper3k>=0.2.8
lxml>=5.1.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# ML and sentiment (torch is NOT included - install separately for GPU compatibility)
numpy<2.0
transformers>=4.37.0
accelerate>=0.26.0
# Note: torch, torchvision, torchaudio should be installed separately
# See instructions at top of this file

# Scheduling
apscheduler>=3.10.4

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
structlog>=24.1.0
pycountry>=23.12.11
setuptools

# For newspaper3k dependencies
Pillow>=10.2.0
nltk>=3.8.1
